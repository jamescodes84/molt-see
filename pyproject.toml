[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "molt-see"
version = "0.1.0"
description = "Vision perception system for AI agents"
requires-python = ">=3.9"

[project.scripts]
moltsee = "cli:main"

[tool.setuptools]
py-modules = ["cli"]
packages = []
